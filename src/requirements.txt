# This file tells Railway/Nixpacks this is a Python project
# Root requirements.txt used by Railway build

# Python 3.12+ compatibility
setuptools>=65.0.0

# Core dependencies for the scheduler
apscheduler==3.10.4
python-arango==7.7.0
pandas==2.1.4
numpy==1.24.0
requests==2.31.0
tenacity==8.2.3
python-dotenv==1.0.0

# OpenAI for embeddings (NEW - required for polymarket pipeline)
openai==1.58.1
httpx==0.27.0

# Additional pipeline dependencies
tqdm==4.65.0
yfinance==0.2.28
scikit-learn==1.3.2

# FRED Federal Reserve Economic Data
fredapi>=0.5.0

# Awards pipeline (embeddings)
torch>=2.0.0
transformers>=4.30.0
