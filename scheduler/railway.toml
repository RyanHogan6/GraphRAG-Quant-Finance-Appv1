# DEPRECATED: Moved to ../railway.toml in repo root
# This file is kept for reference only
# Railway must deploy from root directory to access pipeline code

[build]
builder = "nixpacks"

[deploy]
startCommand = "cd scheduler && python app.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
